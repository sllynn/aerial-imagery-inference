segment-geospatial[samgeo2,samgeo3]==1.3.2
rasterio==1.4.3
supervision==0.27.0
ray[data]==2.41.0
# Re-pinned: a newer opencv pulled in transitively by segment-geospatial /
# supervision SIGABRTs the driver during a module import on this DBR/cluster.
# Holding both wheels at the version we last validated keeps the install
# reproducible.
opencv-python==4.12.0.88
opencv-python-headless==4.12.0.88

# sam3 is installed from git with `--no-deps` so its CUDA-bound deps don't
# shadow DBR's bundled torch. Its pure-Python deps must be supplied here.
timm>=1.0.17     # was timm==1.0.15; bumped to satisfy sam3.
ftfy==6.1.1
iopath>=0.1.10
pycocotools
